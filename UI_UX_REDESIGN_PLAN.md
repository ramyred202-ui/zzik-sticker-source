# 찍스티커 UI/UX 개편안 — 추억 다이어리 중심

> 방향: `스티커 제작 앱`에서 `내 사진과 스티커로 매일을 꾸미는 비공개 디지털 다이어리`로 확장  
> 핵심 대상: 사진 꾸미기, 다꾸, 스크랩북, 하이틴·Y2K·키치 감성을 즐기는 10대 사용자  
> 원칙: 특정 성별을 하나의 취향으로 단정하지 않고 여러 스타일 프리셋을 제공한다.

## 1. 개편의 핵심

현재 앱의 가장 강한 기능은 사진을 오려 나만의 스티커로 만드는 것이다. 하지만 제작한 스티커의 주 사용처가 냉장고 배치와 가챠에 머물러 있어, 사용자가 매일 돌아와야 할 이유는 상대적으로 약하다.

개편 후에는 다음과 같이 역할을 바꾼다.

| 현재 | 개편 후 |
|---|---|
| 사진을 스티커로 만드는 앱 | 사진과 스티커로 하루를 기록하는 앱 |
| 냉장고 보드가 중심 | 달력과 날짜별 다이어리가 중심 |
| 달력은 생성일 확인용 | 달력은 추억을 탐색하는 메인 화면 |
| 꾸미기는 스티커 한 개에 적용 | 꾸미기는 다이어리 한 페이지 전체에 적용 |
| 가챠는 독립 놀이 | 스티커북 안의 보너스 놀이로 이동 |

핵심 제품 문장은 다음과 같이 제안한다.

> **오늘의 사진을 나만의 스티커로 만들고, 하루 한 페이지에 붙여 추억으로 간직해요.**

## 2. 사용자가 느껴야 하는 감정

- `내 것 같다`: 색상, 배경, 글씨, 스티커를 직접 고를 수 있다.
- `쉽다`: 빈 캔버스 앞에서 막히지 않도록 템플릿과 오늘의 질문을 제공한다.
- `귀엽고 재미있다`: 저장할 때 작은 애니메이션과 촉각 반응이 있다.
- `쌓이고 있다`: 달력에 페이지가 하나씩 채워지는 만족감이 있다.
- `안전하다`: 작성한 내용은 기본적으로 비공개이며 기기 안에 저장된다.

## 3. 제안 정보 구조

하단 내비게이션은 5개 영역으로 재구성한다.

```text
┌────────┬──────────┬──────────┬──────────┬────────┐
│  오늘  │ 다이어리 │    ＋    │ 스티커북 │  마이  │
│  home  │ calendar │  create  │ stickers │  me    │
└────────┴──────────┴──────────┴──────────┴────────┘
```

- **오늘**: 오늘의 기록 시작, 질문 카드, 최근 추억
- **다이어리**: 월간 달력과 날짜별 페이지 열람
- **＋ 만들기**: 사진 촬영, 스티커 만들기, 새 다이어리 작성
- **스티커북**: 만든 스티커, 즐겨찾기, 앨범, 보드·가챠
- **마이**: 테마, 백업, 앱 설정, 저장 공간

가운데 만들기 버튼은 다른 탭보다 크고 눈에 띄는 플로팅 버튼으로 배치한다. 누르면 다음 선택지가 아래에서 올라온다.

```text
오늘 무엇을 만들까요?

[ 📔 오늘의 다이어리 ]
[ ✂️ 사진 스티커 ]
[ 📷 빠른 사진 기록 ]
```

## 4. 전체 사용자 흐름

```text
앱 실행
   │
   ├─ 오늘 탭 ── 오늘의 질문/무드 선택 ── 다이어리 편집 ── 저장
   │
   ├─ ＋ 만들기 ── 사진 촬영/선택 ── 배경 제거 ── 스티커북 저장
   │                                           │
   │                                           └─ 바로 다이어리에 붙이기
   │
   ├─ 다이어리 ── 월 선택 ── 날짜 선택 ── 페이지 보기/수정
   │
   └─ 스티커북 ── 스티커 선택 ── 꾸미기/분류/보드 놀이
```

가장 중요한 변화는 스티커 저장 완료 후의 다음 행동이다. 현재처럼 보관함으로만 이동시키지 않고 다음 두 버튼을 제공한다.

- `오늘 다이어리에 붙이기` — 주 행동
- `스티커북에만 저장` — 보조 행동

## 5. 화면별 UI 구조와 배치도

### 5.1 오늘 홈

목적은 앱을 열자마자 오늘의 기록을 시작하게 하는 것이다.

```text
┌──────────────────────────────┐
│  8월 19일 수요일       🔔  ⚙ │
│  오늘은 어떤 하루였나요?     │
│                              │
│  ┌────────────────────────┐  │
│  │  오늘의 무드            │  │
│  │  😊  🥰  😌  😴  😢     │  │
│  └────────────────────────┘  │
│                              │
│  ┌────────────────────────┐  │
│  │ 오늘의 질문             │  │
│  │ “오늘 가장 웃겼던 일은?”│  │
│  │           [기록하기 →]  │  │
│  └────────────────────────┘  │
│                              │
│  최근 추억              모두보기│
│  [어제 페이지] [지난주 페이지] │
│                              │
│ 오늘  다이어리   ＋  스티커북 마이│
└──────────────────────────────┘
```

구성 요소:

- 날짜와 짧은 인사
- 오늘의 무드 5단계 선택
- 매일 바뀌는 짧은 기록 질문
- 작성 중인 페이지 이어 쓰기
- `1년 전 오늘`, `지난주 오늘` 같은 회고 카드
- 최근 작성 페이지 2~3개

첫 버전에서는 날씨처럼 외부 데이터가 필요한 요소는 제외한다.

### 5.2 다이어리 달력

기존 달력을 보조 화면이 아니라 핵심 탐색 화면으로 승격한다.

```text
┌──────────────────────────────┐
│  나의 다이어리          🔍  ⋯ │
│      ‹    2026년 8월    ›    │
│  일   월   화   수   목   금   토│
│ ┌──┬──┬──┬──┬──┬──┬──┐ │
│ │  │  │  │  │  │ 1│ 2│ │
│ │ 3│ 4│📷│ 6│💗│ 8│ 9│ │
│ │10│✨│12│13│14│📔│16│ │
│ │17│18│★ │20│21│22│23│ │
│ └──┴──┴──┴──┴──┴──┴──┘ │
│                              │
│  8월의 기록                  │
│  7일 작성 · 사진 18장 · 😊 4일│
│                              │
│ 오늘  다이어리   ＋  스티커북 마이│
└──────────────────────────────┘
```

개선 사항:

- 날짜 칸에 단순 스티커 썸네일 대신 `다이어리 페이지 미니 프리뷰` 표시
- 무드 이모지 또는 대표 스티커 한 개만 노출해 복잡도 감소
- 작성한 날은 작은 반짝이, 테이프, 하트 등 선택 테마로 강조
- 월간 기록 일수와 대표 무드 요약
- 달력과 `피드 보기` 전환 제공
- 비어 있는 오늘 날짜를 누르면 바로 새 페이지 생성

### 5.3 날짜별 다이어리 보기

한 날짜에 기본 한 페이지를 권장한다. 초기 버전의 데이터 구조와 UI가 단순해지고, `하루 한 장`이라는 제품 습관도 명확해진다.

```text
┌──────────────────────────────┐
│  ‹  8월 19일 수요일     편집  ⋯│
│                              │
│  ╭────────────────────────╮  │
│  │  [사진 프레임]  ✨      │  │
│  │       [내 스티커]       │  │
│  │                        │  │
│  │  오늘 친구랑 떡볶이!   │  │
│  │  너무 매워서 웃겼다 ㅋㅋ│  │
│  │         🎀  washi tape │  │
│  ╰────────────────────────╯  │
│                              │
│  오늘의 무드  🥰             │
│  #친구 #학교 #맛집           │
│                              │
│ 오늘  다이어리   ＋  스티커북 마이│
└──────────────────────────────┘
```

읽기 화면에서는 편집 도구를 숨겨 페이지 자체에 집중하게 한다. 화면 우측 상단에 편집과 더보기만 둔다.

### 5.4 다이어리 편집기

편집기는 자유 배치 캔버스와 쉬운 템플릿을 결합한다.

```text
┌──────────────────────────────┐
│  취소      8월 19일     저장  │
│        ↶  ↷   미리보기        │
│ ┌──────────────────────────┐ │
│ │                          │ │
│ │    선택한 종이 배경       │ │
│ │  [사진] [스티커] “글자”   │ │
│ │       ─ 마스킹테이프 ─    │ │
│ │                          │ │
│ └──────────────────────────┘ │
│                              │
│ [템플릿][사진][스티커][텍스트]│
│ [펜][테이프][배경][프레임]    │
└──────────────────────────────┘
```

편집 도구 우선순위:

1. 템플릿
2. 사진
3. 내 스티커
4. 텍스트
5. 펜·형광펜
6. 마스킹테이프
7. 배경
8. 사진 프레임

캔버스 조작:

- 한 손가락으로 이동
- 두 손가락으로 확대·축소와 회전
- 선택 요소 복제, 앞/뒤 순서 변경, 잠금, 삭제
- 실행 취소·다시 실행
- 화면 밖으로 나갈 때 자동 임시 저장

### 5.5 스티커 만들기

기존 기능을 유지하되 과정을 더 짧고 명확하게 보이게 한다.

```text
사진 선택 → 자동 오려내기 → 가장자리 보정 → 완성
```

결과 화면 구성:

- 전/후 비교 슬라이더
- 지우개와 복원 브러시
- 테두리 두께·색상
- `다이어리에 붙이기` 주 버튼
- `스티커북에 저장` 보조 버튼

기존의 말풍선과 하단 문구는 스티커 자체보다 다이어리 캔버스 요소로 제공하는 편이 재사용성이 높다. 기존 사용자의 데이터 호환을 위해 스티커 꾸미기에서도 계속 표시한다.

### 5.6 스티커북

```text
┌──────────────────────────────┐
│  나의 스티커북          🔍  ＋ │
│  [전체] [최애] [친구] [먹방]  │
│                              │
│  [스티커][스티커][스티커]     │
│  [스티커][스티커][스티커]     │
│                              │
│  오늘의 랜덤 뽑기      [가챠]  │
│  나의 보드             [보기]  │
│                              │
│ 오늘  다이어리   ＋  스티커북 마이│
└──────────────────────────────┘
```

- 검색
- 즐겨찾기
- 앨범 또는 태그
- 최근 사용순·생성일순 정렬
- 선택한 스티커를 다이어리 편집기로 보내기
- 기존 냉장고·화이트보드·가챠는 `나의 보드`와 `놀이` 영역으로 유지

## 6. 비주얼 디자인 방향

### 6.1 전체 톤

기본은 밝은 종이 질감과 둥근 카드 UI를 사용한다. 모든 화면을 분홍색으로 고정하기보다 처음 설정에서 스타일을 고르게 한다.

추천 테마:

- **몽글 파스텔**: 라일락, 크림, 베이비 블루
- **하이틴 핑크**: 핑크, 체리 레드, 실버
- **Y2K 키치**: 라임, 블루, 핑크, 크롬 포인트
- **빈티지 스크랩**: 베이지, 브라운, 낡은 종이 질감
- **심플 모노**: 화이트, 차콜, 한 가지 포인트 색
- **다크 글리터**: 네이비, 퍼플, 별빛 포인트

### 6.2 UI 디테일

- 딱딱한 사각형보다 둥근 카드와 종이 조각 형태 사용
- 버튼 눌림, 스티커 부착, 페이지 저장 시 짧은 탄성 애니메이션
- 탭 아이콘은 선형 시스템 아이콘에 작은 스티커 포인트 추가
- 제목에는 손글씨 느낌을 제한적으로 사용하고 본문은 가독성 높은 고딕 유지
- 마스킹테이프, 폴라로이드, 종이 클립, 찢어진 종이 가장자리를 장식 요소로 사용
- 장식이 콘텐츠를 가리지 않도록 한 화면의 강조색과 장식 종류를 제한

## 7. 다이어리 꾸미기 기능 우선순위

### P0 — 다이어리 MVP

- 날짜별 한 페이지 생성·수정·삭제
- 무드 선택
- 텍스트 입력
- 사진 추가
- 기존 스티커 자유 배치
- 배경 6종
- 기본 템플릿 6종
- 이동·확대·축소·회전·레이어 순서
- 자동 임시 저장과 저장 완료 표시
- 달력 미리보기
- 전체 백업·복원

### P1 — 다꾸 완성도

- 펜·형광펜
- 마스킹테이프와 사진 프레임
- 다양한 글꼴과 글자 효과
- 실행 취소·다시 실행
- 태그와 즐겨찾기
- 오늘의 질문
- `지난해 오늘` 회고
- 페이지를 이미지로 저장·공유
- 스티커 지우개·복원 브러시

### P2 — 반복 사용과 확장

- 주간·월간 다이어리 요약
- 테마 팩과 시즌 장식
- 친구에게 비공개 링크 공유
- 공동 다이어리 또는 교환 일기
- 선택형 계정과 암호화된 동기화
- 출력용 PDF 또는 실물 포토북 주문

## 8. 데이터 구조 변경 제안

스티커 이미지와 다이어리 배치를 분리해야 동일한 스티커를 여러 페이지에서 사용할 수 있다.

### `diaryEntries`

| 필드 | 설명 |
|---|---|
| `id` | 다이어리 고유 ID |
| `dateKey` | 로컬 날짜 `YYYY-MM-DD` |
| `title` | 선택형 제목 |
| `mood` | 무드 코드 |
| `templateId` | 사용 템플릿 |
| `background` | 배경 설정 |
| `tags` | 태그 목록 |
| `createdAt`, `updatedAt` | 생성·수정 시각 |

### `diaryElements`

| 필드 | 설명 |
|---|---|
| `id`, `entryId` | 요소와 소속 페이지 ID |
| `type` | `photo`, `sticker`, `text`, `drawing`, `tape`, `frame` |
| `assetId` | 스티커 또는 사진 자산 참조 |
| `content` | 텍스트 등 요소별 내용 |
| `x`, `y`, `width`, `height` | 기준 캔버스 좌표 |
| `rotation`, `zIndex` | 회전과 레이어 순서 |
| `style` | 색상, 글꼴, 테두리 등 |

### `assets`

원본 사진, 다이어리용 사진, 배경 제거 스티커를 공통 자산으로 관리하면 중복 Blob 저장을 줄일 수 있다. 기존 `stickers` 저장소는 마이그레이션 과정에서 `assets`로 변환하거나 호환 레이어를 둔다.

## 9. 10대 사용자를 위한 안전 원칙

- 모든 다이어리는 기본 비공개
- 공개 프로필과 위치 정보 수집은 초기 범위에서 제외
- 생년월일, 학교, 실명 입력을 요구하지 않음
- 공유할 때 페이지에 위치·촬영 메타데이터가 포함되지 않도록 제거
- 분석 기능은 최소 수집과 명확한 동의를 원칙으로 함
- 연속 작성 보상은 제공할 수 있지만 기록을 못 한 날 죄책감을 주는 표현은 피함
- 공개 커뮤니티를 도입한다면 신고, 차단, 연령 보호 정책을 먼저 설계

## 10. 기존 화면의 이동 계획

| 기존 화면/기능 | 개편 위치 |
|---|---|
| 카메라 탭 | 가운데 `＋ 만들기` 메뉴 |
| 배경 제거 검토 | 스티커 만들기 편집 과정 |
| 보관함 | 스티커북의 전체 목록 |
| 냉장고/화이트보드/문/파티션 | 스티커북의 `나의 보드` |
| 가챠 | 스티커북의 `놀이` |
| 현재 달력 | 새 다이어리 메인 탭으로 확장 |
| 날짜별 스티커 목록 | 날짜별 다이어리 페이지와 사용 자산 목록 |
| 프로필 이름 | 마이 탭의 다이어리 이름 설정 |

기존 기능을 삭제하지 않고 중심 위치만 옮기므로, 현재 사용자가 익숙한 스티커와 가챠 경험을 유지하면서 새 다이어리 경험으로 자연스럽게 확장할 수 있다.

## 11. 추천 구현 순서

### 1차: 화면 구조 개편

1. 새 하단 내비게이션과 오늘 홈 추가
2. 기존 달력을 다이어리 탭으로 이동
3. 기존 카메라를 가운데 만들기 메뉴로 연결
4. 보관함·보드를 스티커북으로 통합
5. 마이 탭과 테마 선택 추가

### 2차: 다이어리 MVP

1. `diaryEntries`, `diaryElements`, `assets` 데이터 구조
2. 날짜별 페이지 생성과 텍스트·사진·스티커 추가
3. 자유 배치 캔버스와 기본 템플릿
4. 자동 임시 저장
5. 달력 미리보기 생성
6. 백업·복원에 다이어리 데이터 포함

### 3차: 다꾸 도구

1. 펜·형광펜
2. 테이프·프레임·글꼴
3. 실행 취소·다시 실행
4. 스티커 가장자리 보정
5. 페이지 이미지 공유

## 12. MVP 완료 기준

- 사용자가 앱을 처음 열고 안내 없이 3분 안에 첫 다이어리를 저장할 수 있다.
- 한 날짜에 텍스트, 사진, 스티커를 넣고 자유롭게 배치할 수 있다.
- 저장 후 앱을 닫았다 다시 열어도 페이지가 동일하게 복원된다.
- 달력에서 작성한 날짜와 페이지 미리보기를 확인할 수 있다.
- 기존에 저장된 스티커가 개편 후에도 모두 보인다.
- 작성 중 앱을 벗어나도 임시 저장본을 복구할 수 있다.
- 다이어리와 스티커 전체를 파일로 백업하고 복원할 수 있다.
- 페이지 공유 시 개인 위치 메타데이터가 포함되지 않는다.

## 13. 최종 제안

1차 개편에서는 소셜 기능이나 많은 장식 팩보다 `오늘 홈 + 날짜별 다이어리 + 기본 꾸미기 + 스티커 연결`에 집중한다. 이 네 가지가 자연스럽게 이어져야 제품이 단순 편집 도구에서 매일 돌아오는 추억 앱으로 바뀐다.

디자인은 10대 사용자가 좋아할 만한 다꾸 감성을 적극 활용하되 하나의 핑크 스타일로 고정하지 않고, 여러 감성 테마 중 자신의 취향을 선택하도록 설계하는 것이 좋다. 제품의 핵심 경쟁력은 제공된 스티커를 붙이는 것보다 **사용자 자신의 사진이 곧 다이어리 스티커가 된다는 점**이다.
